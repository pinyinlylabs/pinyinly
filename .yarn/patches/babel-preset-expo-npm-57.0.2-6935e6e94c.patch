diff --git a/build/plugins/inline-env-vars.js b/build/plugins/inline-env-vars.js
index f8725d3705c7da726a6032bb30e9ca176f557f54..5cefcac3b81cc7aed263a94b1d476229a654dd80 100644
--- a/build/plugins/inline-env-vars.js
+++ b/build/plugins/inline-env-vars.js
@@ -2,10 +2,12 @@
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.expoInlineEnvVars = expoInlineEnvVars;
 const common_1 = require("../common");
+const getenv_1 = require("getenv");
 const debug = require('debug')('expo:babel:env-vars');
 function expoInlineEnvVars(api) {
     const { types: t } = api;
     const isProduction = api.caller(common_1.getIsProd);
+    const strictEnvVars = (0, getenv_1.boolish)('PYLY_STRICT_EXPO_ENV_VARS', false);
     function isProcessEnv(path) {
         const { object } = path.node;
         if (!t.isMemberExpression(object) ||
@@ -46,6 +48,9 @@ function expoInlineEnvVars(api) {
         const key = toMemberProperty(path);
         if (key != null && key.startsWith('EXPO_PUBLIC_')) {
             debug(`${isProduction ? 'Inlining' : 'Referencing'} environment variable in %s: %s`, state.filename, key);
+            if (strictEnvVars && (process.env[key] == null || process.env[key].length === 0)) {
+                throw new Error(`Missing environment variable ${key} for ${state.filename}`);
+            }
             publicEnvVars.add(key);
             if (isProduction) {
                 path.replaceWith(t.valueToNode(process.env[key]));
