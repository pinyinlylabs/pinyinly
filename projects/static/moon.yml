tasks:
  deploy:
    inputs:
      - public/**/*

  generateSpeech:
    command: yarn ts-node-script src/bin/generateSpeech.ts
    inputs:
      - src/**/*
    preset: watcher

tags:
  - prettier
  - typescript
  - vercel
