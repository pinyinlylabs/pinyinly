tasks:
  deploy:
    inputs:
      - public/**/*

  generateSpeech:
    command: yarn tsx --tsconfig tsconfig.json src/bin/generateSpeech.ts
    inputs:
      - src/**/*
    preset: watcher

tags:
  - prettier
  - typescript
  - vercel
