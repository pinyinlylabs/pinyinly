import data from "./grapheme.json";

A component used in some Chinese characters, it is a stroke variant of 乙 (yǐ) — the second Heavenly
Stem. Or as a radical representing the concept of being hidden, used as a component in various
characters.

<WikiHanziGraphemeDecomposition graphemeData={data} />
