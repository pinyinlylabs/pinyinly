import data from "./grapheme.json";

Used to indicate an object is being affected by a verb, or the part of an object designed to be held
in order to use or move it.

<WikiHanziGraphemeDecomposition graphemeData={data} />
