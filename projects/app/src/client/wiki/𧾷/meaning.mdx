import data from "./grapheme.json";

A radical representing a foot, used as a component in other characters.

<WikiHanziGraphemeDecomposition graphemeData={data} />

## Mnemonic

𧾷 is a component form of 足, which depicts a foot (止) attached to a leg.
