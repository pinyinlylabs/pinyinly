import { Text, View } from "react-native";

export default function SupportSettingsPage() {
  return (
    <View className="gap-5">
      {/* Header */}
      <View>
        <Text className="pyly-body-title">Support</Text>
      </View>
    </View>
  );
}
