tasks:
  test:
    command: vitest
    inputs:
      - "**/*"
      - "/.github/workflows" # There is a test that checks workflow environment variables.
      - "/yarn.lock" # Dependencies can change behaviour
    env:
      NODE_OPTIONS: --experimental-sqlite --disable-warning=ExperimentalWarning $NODE_OPTIONS
    toolchain: "node"
